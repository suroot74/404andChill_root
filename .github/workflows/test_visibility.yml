name: Test Workflow Visibility

on:
  workflow_dispatch:  # Enable manual runs
  schedule:
    - cron: '0 8 * * *'  # Daily at 8 AM UTC

jobs:
  test_job:
    runs-on: ubuntu-latest

    steps:
    - name: Run Test Command
      run: echo "Workflow is visible and running!"
